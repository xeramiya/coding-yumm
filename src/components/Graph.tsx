"use client";

import { useState } from "react";
import {} from "lib/api";

type ResasPrefecture = {
  prefCode: number;
  prefName: string;
};

const Graph = async () => {

  return (
    <div>
      <div></div>
    </div>
  );
};

export default Graph;
